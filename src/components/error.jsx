/* eslint-disable react/prop-types */

const Error = ({message}) => {
  return <span className="text-[11px] font-bold text-red-500 mt-1 ml-1 uppercase tracking-tight block animate-in fade-in slide-in-from-top-1">{message}</span>;
};

export default Error;
